# Secret key search service (std_t98_multi_secret_service.py) -- OPTIONAL.
#
# Only needed to break the voice scrambling on encrypted transmissions. The
# audio service detects whether this service is running: without it, clear
# traffic still decodes and plays, and encrypted calls simply stay scrambled.
# A machine that only listens to clear traffic does not need torch at all.
#
# Install torch from the index matching your CUDA/CPU setup; the CPU wheels at
# https://download.pytorch.org/whl/cpu are enough for this workload.

torch
safetensors
